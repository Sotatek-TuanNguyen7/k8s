apiVersion: v1 
kind: Service 
metadata:
  name: service-nodeport
  namespace: my-app
spec:   
  type: LoadBalancer
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000